'use client';import {useEffect,useState} from 'react';import PageHeader from '@/components/PageHeader';import StatCard from '@/components/StatCard';import {Icon} from '@/components/Icons';
export default function Performance(){const [d,setD]=useState(null);useEffect(()=>{fetch('/api/model-performance').then(r=>r.json()).then(setD)},[]);return <section className="page"><PageHeader title="AI model performance" subtitle="Transparent evaluation of the Isolation Forest anomaly detector."/>{!d?<div className="skeleton card"/>:!d.live?<div className="card unavailable"><span><Icon name="chart" size={38}/></span><h2>ML service is offline</h2><p>{d.message}</p><code>cd ml-service &amp;&amp; uvicorn main:app --reload</code><small>No metrics are fabricated. This page populates only from the held-out test set.</small></div>:<><div className="model-status"><span className="live-dot"/> Live model metrics · evaluated on {d.testSamples} held-out samples</div><div className="stats-grid metrics"><StatCard label="ACCURACY" value={pct(d.accuracy)} note="Overall correct predictions"/><StatCard label="PRECISION" value={pct(d.precision)} note="Quality of anomaly flags"/><StatCard label="RECALL" value={pct(d.recall)} note="Anomalies successfully found"/><StatCard label="F1 SCORE" value={pct(d.f1)} note="Precision–recall balance"/></div><div className="model-grid"><div className="card panel"><div className="panel-title"><div><h3>Confusion matrix</h3><p>Predicted class against actual label</p></div></div><div className="matrix"><span/><b>Predicted normal</b><b>Predicted anomaly</b><strong>Actual normal</strong><div className="tn"><em>{d.confusionMatrix.tn}</em>True negatives</div><div className="fp"><em>{d.confusionMatrix.fp}</em>False positives</div><strong>Actual anomaly</strong><div className="fn"><em>{d.confusionMatrix.fn}</em>False negatives</div><div className="tp"><em>{d.confusionMatrix.tp}</em>True positives</div></div></div><div className="card panel explain-metrics"><h3>What these metrics mean</h3><div><b>Precision</b><p>How many flagged transactions were actually anomalous.</p></div><div><b>Recall</b><p>How many anomalous transactions were successfully detected.</p></div><div><b>False positive rate · {pct(d.falsePositiveRate)}</b><p>False positives matter because legitimate users should not be unnecessarily interrupted.</p></div></div></div></>}</section>}
const pct=n=>`${(n*100).toFixed(1)}%`;
